
我的研究应该需要按照以下这些步骤按需来推进：0、构建高效的项目文件夹，避免未来反复修改，逐渐变成“屎山”；1、模拟数据；2、搭建模型（神经网络架构、损失函数等）；3、训练模型；4、模型评估；5、相位恢复结果评估；6、模型的消融实验、超参数调优；7、与传统方法对比。

步骤0：
必须首先完成，这是基础。项目结构必须完善并且可拓展可维护。
我当前已经有了一个项目文件夹， 也算比较系统了。我想让你给我一个完备的方案，然后给我提供一个给claude code的完整提示词，让claude code根据当前已有的以及你拟定的方案，来进行优化整合，并且生成相应的CLAUDE.md文件、.claude文件夹（.claude/context.md​ - 核心模块与业务逻辑摘要、.claude/ignore.md​ - 智能忽略列表等）、根目录下的必要的文件等。目的是每次重新进入文件夹时，claude code能在最大程度节省token消耗的前提下最大程度的理解我的整个项目，发挥最高效的性能。

步骤1：
必须紧接着步骤0之后完成。目前已经确定了需要模拟clean-noisy数据对来进行实验，完成课题。“纯靠实验中得到的噪音数据训练模型，实现去噪”这种方法行不通。
必须要确保实现的任务是：
（1）实现"实验特征校准"模块。必须要得到实验数据的真实参数，尺寸、信号强度、信号分布、噪音强度、噪音分布等，才能真的达到模拟实验数据的效果。
（2）数据质量检查（与实验数据对比）。模拟后的数据需要进行质量检查，确保噪音、信号强度等信息真实贴近实验数据。
已完成的事情：模拟数据模块已经准备好了。但是这两个校准和检查任务还没有独立搭建好。需要完成这两个任务的搭建，并与数据模拟模块进行耦合，完全实现实验数据特征获取、模拟数据、数据质量检查的闭环流程。这部分也可以让claude code来完成，模拟数据模块已经具备了readme文档以及模块自身的CLAUDE.md文件了。
完整的模拟流程是这样的：分析实际实验数据的特征（尺寸、信号强度和分布、噪声强度和分布等），模拟样品形态，根据实验数据的特征模拟衍射以及添加噪音，数据审核。我需要每一步都能够清晰地模块化，因为我需要所有的中间结果让我可以进行分析、论文撰写等。

步骤2：
先按照参考论文中的网络结构、损失函数来设计。

步骤3：
这里我有疑惑，为什么研究方案中有两个任务，“训练基线U-Net（MSE损失）”和“训练物理引导U-Net”。这是什么意思？分别训练两个模型还是对同一个模型先训练MSE损失再训练物理引导损失？还是说其他方式？

步骤4-5（需要着重设计）：
训练好了就需要评估了。
这里的评估需要分为两块：验证数据集上的评估、实验数据的评估。验证数据上表现好是模型训练后必须要具备的，否则训练出来的模型就是所谓的“过拟合”，无法应用；实验数据上去噪效果表现好则是我必须要实现的，不然就没有意义，说明模型缺乏迁移到真实数据上的泛化能力。
这个步骤中，需要确保完成了“评估流程”中涉及到的所有任务：
（1）去噪结果评估
（2）衍射图案层面评估
（3）重构结果评估
已完成的事情：重构算法已经有了，只需要确保所有评估所需的指标设计完成即可。
对于实验数据，参考研究方案中的**3.1 方案一：自相关一致性检验**、3.2 方案二：相位恢复收敛性评估、**3.4 方案四：模拟-实验混合验证（最推荐）**，帮我设计一个完善的方案。“3.3 方案三：Noise2Noise/N2V自监督评估”不采用。
“重构结果评估”我的初步方案是这样：我们实际处理数据时，本就需要重构很多次，才能选出来最好的重构结果。我在想，是不是可以这样，我用相同的参数、相同的support、相同的重构算法去进行重构，然后重构1000次，取最好的100次（或者就统计这1000次），然后统计其平均迭代收敛方差、PRTF等一系列相关指标。

步骤6：
前面的步骤都完成后，需要进行消融实验和超参数调优，找到当前模型的最好效能。
消融实验主要变量是：不同的模拟噪音、模拟尺寸、损失函数等。
噪音分为只添加泊松、添加泊松+高斯、GAN模拟噪音（未来展望，最后再考虑）、泊松+高斯+GAN模拟噪音（未来展望，最后再考虑）等。
损失函数就按照研究方案中所说的，“损失函数各组件”来对比实验。
超参数该怎么调优？
理想效果是，有一张完整的对比表，每一行是不同损失函数、超参数的模型，每一列是所有的评估指标。

步骤7：
最后需要将我最好的模型与传统方法进行对比。同样的，最后需要得到一张完整的对比表，每一行是不同的方法，每一列是所有的评估指标。体现出我的模型是效果最好的。



